<UserControl
    x:Class="GalleryNestApp.View.PhotoGalleryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:GalleryNestApp.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer>
        <ItemsControl x:Name="PhotosItemsControl" ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel
                        MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        ItemHeight="250"
                        ItemWidth="250"
                        Orientation="Horizontal"
                        VirtualizingPanel.IsContainerVirtualizable="True"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="250" 
                        Height="270"
                        Margin="10"
                        PreviewMouseDown="Grid_PreviewMouseDown">
                        
                    <Grid Width="250" Height="250">
                        <ui:ProgressRing
    x:Name="LoadingIndicator"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Visibility="Visible"
                    Foreground="Black"
                            IsIndeterminate="True"
                            IsEnabled="True"
                    Width="40"
                    Height="40"
                    Panel.ZIndex="1"/>
                        <Border ClipToBounds="True" CornerRadius="15">
                            <wv2:WebView2CompositionControl
                                x:Name="PhotoWebView"
                                Width="250"
                                Height="250"
                                Margin="5"
                                DefaultBackgroundColor="Transparent"
                                Loaded="WebView_Loaded"
    Unloaded="WebView_Unloaded"
                                NavigationCompleted="WebView_NavigationCompleted"/>
                        </Border>
                        <ui:Button
                            x:Name="DeleteButton"
                            Width="40"
                            Height="40"
                            Margin="20"
                    Visibility="Hidden"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Panel.ZIndex="10"
                            Background="#312f45"
                            Command="{Binding DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"
                            Icon="{ui:SymbolIcon Symbol=Delete48}" />
                    </Grid>
                        <StackPanel 
                            x:Name="AlbumInfoPanel"
                            VerticalAlignment="Bottom"
                            Margin="0,250,0,0"
                            Visibility="Hidden">
                            <TextBlock 
                                Text="{Binding Name}" 
                                FontWeight="Bold"
                                TextTrimming="CharacterEllipsis"/>

                            <!--<TextBlock 
                                Text="{Binding Photos.Count, StringFormat='{}Фото: {0}'}" 
                                Foreground="Gray"/>-->
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
